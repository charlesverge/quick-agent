{
  "name": "filesystem_append_text",
  "description": "Append UTF-8 text to a file. Creates the file if it does not exist. Returns the file path.",
  "impl": {
    "kind": "python",
    "module": "quick_agent.tools.filesystem.append_text",
    "function": "append_text"
  },
  "input_schema": {
    "type": "object",
    "properties": {
      "path": {"type": "string"},
      "content": {"type": "string"}
    },
    "required": ["path", "content"]
  }
}
