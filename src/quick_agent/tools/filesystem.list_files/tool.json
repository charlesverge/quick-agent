{
  "name": "filesystem_list_files",
  "description": "List files and subdirectories in a directory. Returns a newline-separated list; directory names are suffixed with '/'.",
  "impl": {
    "kind": "python",
    "module": "quick_agent.tools.filesystem.list_files",
    "function": "list_files"
  },
  "input_schema": {
    "type": "object",
    "properties": {
      "directory": {"type": "string"}
    },
    "required": ["directory"]
  }
}
